// 계약서 입력칸 포커스 링: 방입장·프로필(FormInput)과 동일한 두께/투명도로 통일
// (기본 Input의 ring-3 / ring-ring/50 → ring-2 / ring-ring/30 으로 덮어씀)
export const CONTRACT_INPUT_FOCUS =
  'focus-visible:ring-2 focus-visible:ring-ring/30';
